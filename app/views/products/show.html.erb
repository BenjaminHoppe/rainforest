<%= render 'product', this_product: @product  %>
<br/>
<ul>
  <% @reviews.each do |review| %>
  <li><%= render 'reviews/review', review: review %></li>
  <% end %>
</ul>

  <% if flash[:notice] %>
    <div class="notice"><%= flash[:notice] %></div>
  <% end %>

  
  <%= link_to 'Edit',  edit_product_path %> |   <%= link_to("Delete", "/products/#{@product.id}", method: :delete, data: {confirm: "Are you sure you want to delete this?"})  %>
