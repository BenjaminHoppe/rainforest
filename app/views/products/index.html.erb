<h2>Products: <%=@products.all.count%></h2>

<ul>
    <% @products.each do |product| %>
        <li>
            <%= render 'product', this_product: product, link: false  %>
        </li>
    <%end%>
</ul>

<%= link_to 'Add Product',  new_product_path %>